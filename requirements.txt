# Note, this file is fragile. It allows only the basic requirement listing,
# seen below, and full line comments (not end of line comments). This is
# because the file is parsed in setup.py (instead of just by `pip`).

boto>=2.17
docopt>=0.6.1
